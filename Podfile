# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target '上传图片控件' do
  pod "TZImagePickerController"
  pod "SDWebImage"
  pod "AFNetworking"
  pod "Masonry"
end
